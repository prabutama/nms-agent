id: server-16
address: 172.16.0.16

# Device metadata (future use for profiles/collectors).
vendor: linux
model: ""

# Enable real collectors (Phase 5 runtime selection).
icmp:
  enabled: true
snmp:
  enabled: true
  
